<h1><%= @character.name %> </h1>

<img src="<%= @character.photo_url %>" height="300"> 


<a href="/movies/<%= @character.movie_id %>/characters"> Back to the movie</a>
<a href="/movies/<%= @character.movie_id %>/characters/<%= @character.id %>/edit">Update Information</a>

<form action="/movies/<%= @character.movie_id %>/characters/<%= @character.id %>" method="POST" >
<input type="hidden" name="_method" value="DELETE" />
    <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>" />
  <button>Delete this Character</button>
</form>